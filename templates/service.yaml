apiVersion: v1
kind: Service
metadata:
  name: {{ include "axelor-os.fullname" . }}
  labels:
    {{- include "axelor-os.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "axelor-os.selectorLabels" . | nindent 4 }}
